# 🚀 部署到网上（GitHub Pages，免费）

目标效果：**网站公开能看，但只有你能编辑。**

## 前提

- 注册一个 GitHub 账号（github.com，免费）
- 邮箱可以用你的 Outlook 邮箱，但**请用一个新的密码**，不要用聊天里发过的那个

## 步骤（约 5 分钟，推荐方式：自动发布）

### 1. 新建私有仓库
- 登录 github.com → 右上角 **+** → **New repository**
- Repository name 填：`growth-wiki`
- 可见性选 **Private（私有）** ← 关键！这样只有你能改
- 点 **Create repository**

### 2. 上传网站文件
- 在仓库页面点 **Add file → Upload files**
- 打开 `D:\ChatGPT\成长Wiki`，把里面的**所有文件**拖进去
  （包括 index.html、css、docs、js、tools、nav.json、.github 等，注意隐藏的 `.github` 文件夹也要传）
- 点 **Commit changes**

### 3. 开启自动发布
- 仓库页 → **Settings** → 左侧 **Pages**
- Source 选 **GitHub Actions**（不要选 Deploy from a branch）
- 回到仓库页 → **Actions** → 找到 build-and-deploy → 点 **Run workflow** 手动跑一次
- 等 1~2 分钟跑完，你的网站网址就是：
  `https://你的用户名.github.io/growth-wiki/`

### 4. 以后怎么加文章（网站会自动更新！）
- 在 github.com 上打开仓库 → 进入 `docs` 文件夹 → 找到分类
- **Add file → Create new file** → 写 Markdown → **Commit**
- 或者本地写好 .md 后，用 **Upload files** 上传
- 每次提交后，GitHub 会自动：重新生成目录 → 发布网站 → 别人打开就是最新版

## 为什么只有你能编辑

- 仓库是 **Private（私有）**：只有你的账号能进去改文件
- 网站是 **Public（公开）**：任何人都能打开看
- 别人想看可以，想改？没有入口，也进不了你的仓库 😎

## 本地预览（可选）

- 双击 `D:\ChatGPT\成长Wiki\本地预览.bat`，浏览器会打开本地效果
- 改完 `docs` 里的文章，刷新浏览器就能看到，确认没问题再上传到 GitHub

## 进阶（以后可以学）

- 装 GitHub Desktop 或学 Git 命令，本地编辑 + 一键推送，体验更好
- 想要「登录密码才能看」的完全私密网站？需要付费托管或自建服务器，暂时不需要