# 📝 我的网站 — 编辑指南

这个文件夹就是你的网站内容。改完里面的文字，跟我说一声「上传网站」，我就帮你发布。

## 文章在哪里改

文章在 `docs` 文件夹里，按分类放：

| 文件夹 | 内容 |
| --- | --- |
| docs/01-关于我 | 关于我 |
| docs/02-成长记录 | 成长时间线、每月总结 |
| docs/03-学习笔记 | 学习笔记、路线图、刷题方法…… |
| docs/04-我的项目 | 项目介绍 |
| docs/05-工具 | 工具页 |

## 怎么改文字

1. 用 **Typora** 或 **VS Code** 打开 `docs` 里的 `.md` 文件
2. 直接修改文字，保存（Ctrl + S）
3. 跟我说一声「上传网站」，我帮你发布；或者你自己打开终端执行 `git add .` + `git commit` + `git push`

## 怎么加一篇新文章

1. 在对应分类文件夹里新建一个 `.md` 文件（比如 `docs/03-学习笔记/07-我的Python笔记.md`）
2. 开头写 `# 标题`，下面写内容
3. 告诉我，我帮你加上目录并发布

## 本地预览

双击 `本地预览.bat`，浏览器打开就能看到效果（改完刷新即可）。

## 小提醒

- 文件开头第一个字必须是 `# ` 标题，目录才会显示文章名
- 不要在 `nav.json` 里手动改，我会自动更新它